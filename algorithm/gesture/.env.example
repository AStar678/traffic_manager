GESTURE_ALGORITHM_HOST=0.0.0.0
GESTURE_ALGORITHM_PORT=8002
GESTURE_ALGORITHM_THREADS=4

# 通常无需覆盖，默认使用本目录中的 config/ 与 data/。
# GESTURE_CONFIG_PATH=/absolute/path/to/gesture/config/gesture-config.json
# GESTURE_PROTOTYPE_STORE=/absolute/path/to/gesture/data/gesture_prototypes.json
