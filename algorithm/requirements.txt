# ================================
# VisionDrive 算法服务依赖
# 基于《算法技术路线》文档
# ================================

# --- Web 框架 ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic>=2.0.0

# --- 车牌识别：CLPRNet ---
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
pillow>=10.0.0

# --- 手势识别：替换为前端 MediaPipe 关键点 + 原型网络服务 ---
# 算法服务只接收关键点特征向量，不再依赖旧 Python 手势模型。

# --- 通用 ---
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# --- LLM 客户端（告警摘要）---
openai>=1.0.0
