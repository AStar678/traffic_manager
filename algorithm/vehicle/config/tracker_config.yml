# PP-Vehicle 官方方案使用 OC-SORT。独立配置避免受
# PaddleDetection 通用 tracker_config.yml 默认类型变更的影响。
type: OCSORTTracker

OCSORTTracker:
  det_thresh: 0.4
  max_age: 30
  min_hits: 3
  iou_threshold: 0.3
  delta_t: 3
  inertia: 0.2
  min_box_area: 0
  vertical_ratio: 0
  use_byte: false
  use_angle_cost: false
